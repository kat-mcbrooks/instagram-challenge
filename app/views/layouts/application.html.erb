<!DOCTYPE html>
<html>
  <head>
    <title>MyGram</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #EB984E">
    <div class="container-fluid p-3" >
      <a class="navbar-brand" href="<%= posts_path %>">
        <img class="filter-grey" src="/images/camera-retro-solid.svg" height='60'>  
      </a>
      <p class="header align-text-bottom">MyGram</p>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
      </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <% if user_signed_in? %> 
 
        <ul class="navbar-nav ms-auto">
        <li>
          <%= link_to "My profile", user_path(current_user), class: "navlink" %>
      </li>   
      <li>
        <%= link_to "Sign out", destroy_user_session_path, class: "navlink", 'data-turbo-method': :delete %>
      </li>  
      <% else %>
        <ul class="navbar-nav ms-auto">
          <li>
            <%= link_to "Sign in", new_user_session_path, class: "navlink" %>
          </li>   
          <li>
            <%= link_to "Sign up", new_user_registration_path, class: "navlink" %>
          </li>  
      <% end %>
 

      </div>
    </div>
  </nav>

  

  <% if notice %>
    <p class="alert alert-success"><%= notice %></p>
  <% end %>
  <% if alert %>
    <p class="alert alert-danger"><%= alert %></p>
  <% end %>
    <%= yield %>
  </body>
</html>
